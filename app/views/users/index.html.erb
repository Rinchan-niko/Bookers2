<h1>Users 一覧</h1>

<%= render 'list', user: @user %>

<% @users.each do |user| %>
  <div>
    <%= image_tag user.get_profile_image(100,100) %>
    <p><%= user.name %></p>
    <li>
      <%= link_to "Show", user_path(user.id) %>
    </li>
  </div>
<% end %>